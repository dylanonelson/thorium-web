# Copy this file to .env before running npm scripts.
# READER_API_URL should point to the reader_api instance that exposes /ask.
READER_API_URL="http://localhost:8000"  # TODO: Replace with the dev or staging base URL hosting /ask.

# Pick the Ollama model that balances speed and judging quality on your machine.
OLLAMA_MODEL="llama3"  # TODO: Update to whichever local judge model you install (e.g., mistral, llama3:70b).
